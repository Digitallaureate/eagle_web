import DummyPage from '@/components/DummyPage';

export default function ResourcesPage() {
  return (
    <DummyPage
      eyebrow="Resources"
      title="Resources"
      description="This is the main resources placeholder page. We can use this route as an entry point for media, press releases, gallery content, and blog posts."
    />
  );
}
