import DummyPage from '@/components/DummyPage';

export default function GalleryPage() {
  return (
    <DummyPage
      eyebrow="Resources"
      title="Gallery"
      description="This is the placeholder page for Gallery. We can later build it into a photo and video showcase for events, drones, facilities, and training."
    />
  );
}
